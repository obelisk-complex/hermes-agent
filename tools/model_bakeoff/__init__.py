"""Model bakeoff: an offline-first harness that evaluates a fixed roster of LLMs
on a curated coding corpus (correctness + latency + cost) and proposes a
quality-gate model ladder. Indicative, not authoritative (see SPEC.md §13)."""
