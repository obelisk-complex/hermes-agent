Implement `digit_root(n: int) -> int` in `solution.py`.

Repeatedly sum the decimal digits of a non-negative integer `n` until a single
digit (0 to 9) remains, and return it. For example `digit_root(9999)` is `9`
(9+9+9+9 = 36, then 3+6 = 9). `digit_root(0)` is `0`.
