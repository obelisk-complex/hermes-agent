Implement `isqrt(n: int) -> int` in `solution.py`.

Return the integer square root of a non-negative integer `n`: the largest integer
`r` such that `r * r <= n`. The result must be EXACT for arbitrarily large `n`,
including values far beyond the range where 64-bit floating point stays exact.
Raise `ValueError` for negative `n`.
