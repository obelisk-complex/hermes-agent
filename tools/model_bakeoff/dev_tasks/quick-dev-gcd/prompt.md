Implement `gcd(a: int, b: int) -> int` in `solution.py`.

Return the greatest common divisor of two non-negative integers `a` and `b`. By
convention `gcd(0, 0)` is `0`. The result is always non-negative.
