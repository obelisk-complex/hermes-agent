# Available Models

See `~/.hermes/wiki/` for current model availability, or use `hermes models list`.
OpenCode Zen: `/zen/go/v1` (18 models, no Claude — default), `/zen/v1` (46 models, includes Opus).
