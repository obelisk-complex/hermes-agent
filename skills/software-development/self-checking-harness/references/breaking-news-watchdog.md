# Breaking News Watchdog

For recurring news monitoring, use `cronjob` with `no_agent: True` and a script that polls feeds. Silent when no new items, alerts on breaking news.

15-minute interval, script-based watchdog pattern.
